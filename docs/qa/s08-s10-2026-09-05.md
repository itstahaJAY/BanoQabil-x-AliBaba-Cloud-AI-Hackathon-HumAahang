# S08–S10 follow-up — 2026-09-05

## Implemented

- S08: Installed SDK-compatible `expo-dev-client`, set local Android app ID `com.humahang.mobile`, added `android:build` / `start:device` scripts. Kept honest missing-module guidance. Expo Go cannot acquire speech recognition through a JavaScript reload.
- S09: Removed fixed demo sentences. Continuous recognition commits final segments; Pause calls stop and waits for completion; Resume starts another session without clearing earlier text. Natural engine end leaves captions paused. Single-utterance chat dictation keeps its existing review-before-send contract. A paused editor permits typing/keyboard dictation. Save writes a text-only snapshot to versioned AsyncStorage, updates the same screen session instead of duplicating it, and reports write errors without clearing text. History lists and expands these actual saved transcripts separately from demo chat shortcuts.
- S10: Category chips now filter the phrase records. Shared compact recording-status styling replaces the bulky player, with Preparing voice / Speaking tied to actual callbacks. Stop, replacement, errors, startup timeout, backgrounding and navigation cleanup invalidate old callbacks. Enumerating voices on entry initializes Expo's lazy Android TTS engine before a tap; no silent/fake audio is played. Custom text can be spoken immediately (not persisted as a favorite).
- `.env` is now ignored by Git. No secret values were read or placed in the app.

UI UX Pro Max guided compact feedback, explicit loading/disabled states, vector icons, readable phrase typography, press feedback and 48-unit touch targets. Ponytail guided reuse of Expo Speech, the existing recording card, recognition controller and AsyncStorage. No new UI/state framework was added.

## Evidence

- `npx tsc --noEmit`: passed after final TSX changes.
- `npm test`: 22 passed. Two continuous-recognition tests first failed against the old single-utterance implementation, then passed. Other tests cover filters, playback startup/stale callbacks/Stop, and storage persistence/error handling.
- Web export: `.expo/s08-s10-web-check`; Android JS export: `.expo/s08-s10-android-check`. Both passed before the final short-screen layout adjustment; final layout was type-checked and browser-checked afterward. These are NOT native APK builds.
- Browser: Travel reduced the grid to one washroom phrase; matching Speaking state observed. Fictional text `QA-only S09: saved transcript check.` was edited, saved, reopened in History, survived a full reload and expanded to the full text. One clearly labelled QA record remains in this browser's local storage.
- Browser: larger-text toggle, 375×812 portrait and 812×375 landscape caption layout checked. Found and fixed short-screen collapse; landscape controls can be reached by scrolling. Removed the introduced flex shorthand/longhand conflict. Existing web shadow-style deprecation warning remains.
- Installed Android tooling found: Android Studio JBR 21 and SDK platform/build tools. `adb devices` returned no connected devices. No native build, installation or live microphone capture was performed.

## Still blocked / not verified

- **Translation:** no existing backend endpoint/auth contract was found or supplied. Translate is disabled with a visible explanation, not implemented as a fake success. A server-side DeepSeek text-translation connection remains to be built or connected. Deepgram STT is a separate audio-to-text service; it is not currently integrated. Provider secrets must not go in Expo public environment variables or client code.
- **Physical S08/S09:** install the native development build, grant permissions, test English/Urdu, pause/resume and foreground/background on the actual phone. See [Android setup](../qa-guide.md#android-native-build-on-this-pc).
- **Physical S10 timing:** native first-play delay is not measured. Early engine initialization can reduce cold-start wait but does not guarantee instant device/network voices. On-device audio output and silent-mode/media-volume behavior still require testing.
- Dynamic Type at largest system size, screen readers, dark-mode support and physical keyboard overlap were not runtime-verified. The existing app uses a light palette. The reused waveform already respects reduced motion; OS reduced-motion behavior was not retested.
- npm reported 13 moderate dependency advisories; no broad automatic dependency upgrades were attempted.

The [QA guide](../qa-guide.md) now distinguishes actual behavior, demo shortcuts, and blockers. Saved transcripts are local, not encrypted by this app, not audio recordings, and not synchronized across devices.
